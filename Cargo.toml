[package]
name = "navirust"
version = "0.1.0"
edition = "2021"

# Mova tudo para cá:
[dependencies]
reqwest = { version = "0.11", features = ["json"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
md5 = "0.7"
hex = "0.4"
